# inverse-positioning

Synthetic inverse-positioning notebook using a uv-managed Python environment.

Run the notebook server:

```powershell
uv run jupyter notebook inverse_positioning_entropy.ipynb
```

Re-execute the larger experiment notebook in place:

```powershell
uv run jupyter execute --inplace --timeout=300 inverse_positioning_entropy.ipynb
```
